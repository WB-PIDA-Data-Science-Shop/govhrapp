% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server_utils.R
\name{lookup_meso_table}
\alias{lookup_meso_table}
\title{Look Up a Slice of a Meso Table}
\usage{
lookup_meso_table(
  meso_table,
  group_var_value,
  subgroup_filter = NULL,
  date_range = NULL
)
}
\arguments{
\item{meso_table}{A meso table (or a slice of one).}

\item{group_var_value}{Character string naming the grouping variable to look up,
e.g. \code{"ref_date"}, \code{"paygrade"}, or \code{"gender"}.}

\item{subgroup_filter}{Character vector of subgroup values to keep, or
\code{NULL} to keep every subgroup available for \code{group_var}.
Ignored when \code{group_var == "ref_date"}.}

\item{date_range}{A length-2 vector of dates (or values coercible to
\code{Date}) giving the inclusive range of \code{ref_date} to keep, or \code{NULL} to
keep every reference date.}
}
\value{
The subset of \code{meso_table} matching \code{group_var} (and, if supplied,
\code{subgroup_filter} and \code{date_range}).
}
\description{
Filters a meso table (as produced by \code{\link[=build_analytics_meso_table]{build_analytics_meso_table()}},
\code{\link[=build_workforce_meso_table]{build_workforce_meso_table()}}, or \code{\link[=build_wagebill_meso_table]{build_wagebill_meso_table()}}) down to
the rows needed to drive a single plot: one grouping variable, optionally
a set of subgroups, and optionally a reference-date range. This is the
core building block that lets plot pipelines simply \emph{look up}
pre-computed indicators (\code{headcount}, \code{wagebill}, \code{percentile_distribution},
\code{decile_distribution}, ...) instead of recomputing them from raw
microdata every time a control changes.
}
