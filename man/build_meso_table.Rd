% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server_utils.R
\name{build_meso_table}
\alias{build_meso_table}
\title{Build a Meso-Level Summary Table}
\usage{
build_meso_table(
  workforce_data,
  wagebill_data,
  group_vars = NULL,
  wagebill_measure = "gross_salary_lcu",
  percentile_binwidth = 100,
  scalars = list(headcount = function(data) nrow(dplyr::distinct(data,
    .data[["personnel_id"]]))),
  vectors = list(percentile_distribution = function(data) {
     if (nrow(data) == 0) {
 
           return(NULL)
     }
     compute_percentile(data, measure_col =
    wagebill_measure, binwidth = percentile_binwidth)
 }, decile_distribution =
    function(data) {
     if (nrow(data) == 0) {
         return(NULL)
     }
    
    compute_decile(data, measure_col = wagebill_measure)
 })
)
}
\arguments{
\item{workforce_data}{A data frame with workforce/personnel attributes
(headcount), containing \code{ref_date}.}

\item{wagebill_data}{A data frame with contract/salary attributes (wage
bill), containing \code{ref_date} and \code{wagebill_measure}.}

\item{group_vars}{Character vector of grouping columns to build meso cells
for, e.g. \code{c("ref_date", "paygrade", "gender")}. Defaults to \code{"ref_date"}
plus every character grouping column common to both \code{workforce_data} and
\code{wagebill_data}, as identified by \code{\link[=identify_group_choices]{identify_group_choices()}}.}

\item{wagebill_measure}{Character string naming the wage column to
summarize in \code{wagebill_data}. Default \code{"gross_salary_lcu"}.}

\item{percentile_binwidth}{Numeric bin width passed to
\code{compute_percentile()} when building \code{percentile_distribution}. Default
\code{100}.}

\item{scalars}{A named list of functions, each taking a single-cell data
frame (all rows for one \code{ref_date}/\code{group_var}/\code{subgroup} combination,
from \code{workforce_data}) and returning a single scalar. Defaults to
\code{headcount}, the row count.}

\item{vectors}{A named list of functions, each taking a single-cell data
frame (from \code{wagebill_data}) and returning an arbitrary vector (typically
a data frame) to store in a list-column. Defaults compute
\code{percentile_distribution} via \code{compute_percentile()} and
\code{decile_distribution} via \code{compute_decile()}.}
}
\value{
A tidy data frame (the "meso table") with one row per
\code{ref_date} / \code{group_var} / \code{subgroup} combination, containing:
\itemize{
\item \code{ref_date}, \code{group_var}, \code{subgroup}: row identifiers.
\item One column per entry in \code{indicators} (scalar), plus \code{wagebill}
(scalar, sum of \code{wagebill_measure}).
\item One list-column per entry in \code{objects}, each holding a nested
data object for that particular slice.
}
}
\description{
Consolidates the repeated "filter data -> compute summary -> feed a plot"
pipelines found throughout the govhr dashboard servers (see e.g.
\code{\link[=workforce_overview_server]{workforce_overview_server()}}, \code{\link[=wagebill_overview_server]{wagebill_overview_server()}},
\code{\link[=wagebill_equity_server]{wagebill_equity_server()}}) into a single tidy table. Each row of the
resulting table represents one meso-level slice of the data: a reference
date, a grouping variable, and a value of that grouping variable (a
"subgroup"). Each column is either a scalar \emph{indicator} (e.g. \code{headcount},
\code{wagebill}) or a nested \emph{data object} (e.g. \code{percentile_distribution},
\code{decile_distribution}) computed for that particular slice.
}
\details{
Internally, this combines \code{\link[=build_workforce_meso_table]{build_workforce_meso_table()}} and
\code{\link[=build_wagebill_meso_table]{build_wagebill_meso_table()}}; call those directly when only one side is
needed (e.g. to avoid rebuilding workforce indicators every time a
wagebill measure changes).
}
\examples{
\dontrun{
meso_table <- build_meso_table(workforce_data, wagebill_data)

# a single slice's percentile distribution:
meso_table$percentile_distribution[[1]]

# look up just the "paygrade" trend, without recomputing anything:
lookup_meso_table(meso_table, group_var = "paygrade")
}

}
