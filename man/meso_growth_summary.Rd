% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server_utils.R
\name{meso_growth_summary}
\alias{meso_growth_summary}
\title{Compute a Growth Rate from a Meso Table Slice}
\usage{
meso_growth_summary(meso_slice, group_var, value_col)
}
\arguments{
\item{meso_slice}{A data frame returned by \code{\link[=lookup_meso_table]{lookup_meso_table()}}, already
labeled via \code{\link[=label_subgroup]{label_subgroup()}} so that it contains a column named
\code{group_var}.}

\item{group_var}{Character string naming the grouping column.}

\item{value_col}{Character string naming the indicator column to compare,
e.g. \code{"headcount"} or \code{"wagebill"}.}
}
\value{
A data frame with \code{group_var} and a \code{growth_rate} column
(percentage points, e.g. \code{12.5} for +12.5\%).
}
\description{
Computes the percentage change in \code{value_col} between the earliest and
latest reference dates present in \code{meso_slice}, separately for each
subgroup. Operates on already-aggregated meso-level indicators (e.g.
\code{headcount}, \code{wagebill}) rather than raw microdata, since a meso table
slice already has one row per \code{ref_date}/subgroup combination.
}
