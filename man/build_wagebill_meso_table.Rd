% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server_utils.R
\name{build_wagebill_meso_table}
\alias{build_wagebill_meso_table}
\title{Build a Wagebill Meso Table}
\usage{
build_wagebill_meso_table(wagebill_data, group_vars = NULL, indicators = NULL)
}
\arguments{
\item{wagebill_data}{A data frame with contract/salary attributes,
containing \code{ref_date} and \code{wagebill_measure}.}

\item{group_vars}{Character vector of grouping columns to build meso cells
for. Defaults to \code{"ref_date"} plus every character grouping column
identified by \code{\link[=identify_group_choices]{identify_group_choices()}}
\code{100}.}

\item{indicators}{A named list of functions, each taking a single-cell data
frame and returning a vector. Defaults to \code{wagebill}, the sum of
\code{wagebill_measure} columns.}
}
\value{
A tidy data frame with \code{ref_date}, \code{group_var}, \code{subgroup},
\code{wagebill}, and one list-column per entry in \code{vectors}.
}
\description{
Builds the wagebill half of a meso table (see \code{\link[=build_analytics_meso_table]{build_analytics_meso_table()}}): one
row per \code{ref_date}/\code{group_var}/\code{subgroup} cell, with a \code{wagebill} scalar
plus one list-column per entry in \code{vectors}. Standalone from
\code{\link[=build_workforce_meso_table]{build_workforce_meso_table()}} so that it can be rebuilt on its own
whenever \code{wagebill_measure} changes, without touching workforce data.
}
\examples{
build_wagebill_meso_table(govhr::bra_hrmis_contract)

}
